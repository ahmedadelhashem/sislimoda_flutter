// import 'package:url_launcher/url_launcher.dart';

// class LaunchUrl {
//   ///[url] the url you need to run
//   static launchUrl(url) async {
//     if (await canLaunch(url)) {
//       await launch(url);
//     } else {
//       //////print('Could not launch $url');
//     }
//   }
// }
